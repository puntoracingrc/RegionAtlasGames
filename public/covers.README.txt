Las portadas no van en git.

Hosting público (web):
  https://www.puntoracing.net/MEDIAPUNTORACINGWEB/MEDIAREGIONATLAS/covers/
    {plataforma}/{slug-del-titulo}.jpg

Local (desarrollo): symlink al disco externo
  public/covers -> /Volumes/Nuevo vol/pal-es-retro/covers

  npm run storage:setup

Override opcional (.env.local / Vercel):
  NEXT_PUBLIC_COVERS_BASE_URL=https://www.puntoracing.net/MEDIAPUNTORACINGWEB/MEDIAREGIONATLAS/covers

Reglas de nombre:
  - Solo el título del juego (slug ASCII: animal-crossing-new-leaf.jpg)
  - Sufijo -2, -3 solo si hay otro juego con el mismo título en la misma plataforma

Normalizar catálogo (quitar URLs externas en coverUrl):
  python3 scripts/normalize_catalog_cover_urls.py
